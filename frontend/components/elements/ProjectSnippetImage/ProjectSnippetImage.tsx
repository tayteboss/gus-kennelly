import styled from 'styled-components';

const ProjectSnippetImageWrapper = styled.div``;

const ProjectSnippetImage = () => {
	return (
		<ProjectSnippetImageWrapper>
			ProjectSnippetImage
		</ProjectSnippetImageWrapper>
	);
};

export default ProjectSnippetImage;
